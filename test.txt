Remove and return item at index (default last).

Raises IndexError if list is empty or index is out of range.

(Удалить и вернуть элемент по индексу (по умолчанию).
Повышает индексерр, если список пуст или индекс не в диапазоне.)
